<%= form_for [@review, @comment] do |f| %>

  <div class="form-group">
    <%= f.label :title %>
    <%= f.text_field :body, autofocus: true, class: "form-control" %>
  </div>
  
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>