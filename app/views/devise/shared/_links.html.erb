<% unless controller_name == "sessions" %>
  <%= link_to t("application.login"), new_session_path(resource_name) %>
<% end %>

<% unless devise_mapping.registerable? && controller_name == "registrations" %>
  <%= link_to t("application.sign_up"), new_registration_path(resource_name) %>
<% end %>
